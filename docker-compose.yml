services:
  playwright-api:
    build: .
    container_name: playwright-api
    shm_size: '2gb'
    ports:
      - "8080:8080"
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - my-project_web_network

networks:
  my-project_web_network:
    external: true
